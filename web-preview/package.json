{
  "name": "wxapp-web-preview",
  "version": "0.1.0",
  "private": true,
  "description": "Browser preview for /src/ WeChat mini program (vibe coding dev only)",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3006 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3006"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
